# Etherscan API key used by the current client-side history integration.
# This value is NOT a true secret in a browser build. For production,
# move Etherscan requests to a backend/serverless proxy and keep the key there.
REACT_APP_ETHERSCAN_API_KEY=
